import 'package:flutter/material.dart';

class AppColors {
  static const bg = Color.fromARGB(255, 196, 206, 210);
  static const white = Colors.white;
  static const black = Colors.black;
  static const ltOrange = Color.fromARGB(255, 255, 240, 235);
}
